services:
  - type: web
    name: novaric-backend
    env: python
    region: frankfurt
    rootDir: novaric-backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: USE_LIVE_DB
        value: "True"
